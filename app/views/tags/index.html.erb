<h1>All Tags</h1>

<ul id="tags">
   <% @tags.each do |tag| %>
      <li>
      	<%= link_to tag.name, tag_path(tag) %> &nbsp<%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "sure?!"} %>
      </li>
   <% end %>
</ul>